import UserLoginForm from './UserLoginForm';

const Header = () => {
    return (
        <header>
            <h1>CocktailFinder</h1>
            <UserLoginForm />
        </header>
    );
}

export default Header;